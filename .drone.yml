kind: pipeline
name: default
type: exec
steps:
- name: test
  image: golang:latest
  commands:
  - go test -race ./...
  - go build

trigger:
  branch:
    - main
  event:
    - pull_request
    - push
